/*
#include "get_next_line.h"

char    *get_next_line(int fd)
{
   int    bytes_read;
   char    *buf;

   buf = ft_calloc(3 + 1, sizeof(char));
   if (!buf)
        return (NULL);
    bytes_read = read(fd, buf, 3);
        
}
*/